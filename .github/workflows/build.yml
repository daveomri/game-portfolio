name: gpp-build

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
      - name: Install Dependencies
        run: npm install
      - name: Build project
        run: npm build
